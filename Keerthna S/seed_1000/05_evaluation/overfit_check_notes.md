# Overfitting Check Notes

Below is a summary of the overfitting/underfitting diagnosis for each model:

- **Naive**: POSSIBLE OVERFITTING (validation error much worse than training fit)
- **ARIMA**: POSSIBLE OVERFITTING (validation error much worse than training fit)
- **Analog-Only**: POSSIBLE OVERFITTING (validation error much worse than training fit)
- **Bass-Only**: POSSIBLE OVERFITTING (validation error much worse than training fit)
- **Analog+Bass (Static)**: POSSIBLE OVERFITTING (validation error much worse than training fit)
- **Analog+Bass (Adaptive)**: POSSIBLE OVERFITTING (validation error much worse than training fit)
